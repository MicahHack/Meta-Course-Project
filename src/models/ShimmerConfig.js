export default class ShimmerConfig {
    constructor(height = "350px", width = "300px", borderRadius = "7") {
        this.height = height;
        this.width = width;
        this.borderRadius = borderRadius;
    }
}